---
globs: ["vscode-extension/src/**/*.ts", "vscode-extension/test/**/*.ts"]
---

# TypeScript Conventions

- Use strict mode typing — avoid `any` unless interfacing with untyped external data
- Prefer `interface` over `type` for object shapes
- All exported functions must have explicit return types
